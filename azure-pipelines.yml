variables:
  STACK_ROOT: /home/vsts/.stack
  tag: '$(Build.BuildId)'

stages:
- stage: Build
  displayName: Build
  jobs:
  - job: Build
    pool:
      vmImage: 'ubuntu-latest'
    steps:
    - task: CacheBeta@0
      displayName: Download stack root cache
      inputs:
        key: |
          stack
          $(Agent.OS)
          $(Build.SourcesDirectory)/stack.yaml
          $(Build.SourcesDirectory)/package.yaml
        path: '.azure-cache'
        cacheHitVar: 'CACHE_DOWNLOADED'
    - script: |
        mkdir -p $STACK_ROOT
        tar -xf .azure-cache/stack-root.tar.gz -C /
        mkdir -p .stack-work
        tar -xf .azure-cache/stack-work.tar.gz
      displayName: Extract cache
      condition: eq(variables.CACHE_DOWNLOADED, 'true')
    - script: |
        mkdir -p ~/.local/bin
        curl -L https://get.haskellstack.org/stable/linux-x86_64.tar.gz | \
          tar xz --wildcards --strip-components=1 -C ~/.local/bin '*/stack'
      displayName: Install stack
    - script: |
        export PATH=$HOME/.local/bin:$PATH
        stack setup
      displayName: Install GHC
    - script: |
        export PATH=$HOME/.local/bin:$PATH
        mkdir -p bin/lib
        stack build --copy-bins --local-bin-path ./bin
        stack build hlint weeder
        stack exec hlint -- .
        stack exec weeder -- . --build
        strip ./bin/impatience-exe
        ldd ./bin/impatience-exe
      displayName: Build Non-Tests
    - publish: bin/impatience-exe
      artifact: impatience-exe
    - script: |
        export PATH=$HOME/.local/bin:$PATH
        stack build --test --no-run-tests --coverage
        cp $(stack path --dist-dir)/build/impatience-test/impatience-test ./bin/impatience-test
      displayName: Build Tests
    - publish: bin/impatience-test
      artifact: impatience-test
    - script: |
        mkdir -p .azure-cache
        tar -cf .azure-cache/stack-root.tar.gz $STACK_ROOT
        tar -cf .azure-cache/stack-work.tar.gz .stack-work
      displayName: Build Cache
      condition: eq(variables.CACHE_DOWNLOADED, 'false')
    - publish: deploycfg
      artifact: manifests
- stage: Test
  displayName: Test
  dependsOn: Build
  jobs:
  - job: Test
    pool:
      vmImage: 'ubuntu-latest'
    steps:
    - download: current
      artifact: impatience-test
    - script: |
        mkdir -p ~/.local/bin
        curl -L https://get.haskellstack.org/stable/linux-x86_64.tar.gz | \
          tar xz --wildcards --strip-components=1 -C ~/.local/bin '*/stack'
      displayName: Install stack
    - script: |
        export PATH=$HOME/.local/bin:$PATH
        cp $(Pipeline.Workspace)/impatience-test .
        ls -l -R
        ./impatience-test
        stack hpc report impatience-test.tix
      displayName: Execute Tests (w/coverage)
  - job: ImageTest
    pool:
      vmImage: 'ubuntu-latest'
    steps:
    - download: current
      artifact: impatience-test
    - download: current
      artifact: impatience-exe
    - script: |
        cp Dockerfile Dockerfile.test
        mkdir -p bin
        cp -a $(Pipeline.Workspace)/. ./bin/
        ls -l -R
        echo "COPY $(Pipeline.Workspace)/impatience-test /app/impatience-test" >> Dockerfile.test
        docker build -t "test-image" -f Dockerfile.test .
        docker run test-image impatience-test
      displayName: Execute Tests In Image
- stage: Push
  displayName: Push images
  dependsOn: Build
  jobs:
  - job: Push
    displayName: Push image to registry
    condition: and(succeeded(), eq(variables['Build.SourceBranch'], 'refs/heads/master'))
    pool:
      vmImage: 'ubuntu-latest'
    steps:
    - download: current
      artifact: impatience-exe
    - script: |
        mkdir -p ./bin
        cp $(Pipeline.Workspace)/impatience-exe ./bin/impatience-exe
    - task: Docker@2
      displayName: Build and Push Image
      inputs:
        containerRegistry: 'impatienceregistry'
        repository: 'impatience'
        command: 'buildAndPush'
        Dockerfile: '**/Dockerfile'
        tags: $(tag)
- stage: Deploy
  displayName: Deploy to k8s
  dependsOn: |
  - Test
  - Push
  condition: and(succeeded(), eq(variables['Build.SourceBranch'], 'refs/heads/master'))
  jobs:
  - deployment: Deploy
    displayName: Deploy to k8s
    pool:
      vmImage: 'ubuntu-latest'
    environment: 'impatience.default'
    strategy:
      runOnce:
        deploy:
          steps:
          - task: KubernetesManifest@0
            displayName: Create image pull secret
            inputs:
              action: 'createSecret'
              kubernetesServiceConnection: 'impatienceKubernetes'
              namespace: 'default'
              secretType: 'dockerRegistry'
              secretName: 'impatiencepullsecret'
              dockerRegistryEndpoint: 'impatienceregistry'
          - task: KubernetesManifest@0
            displayName: Deploy to k8s cluster
            inputs:
              action: deploy
              kubernetesServiceConnection: 'impatienceKubernetes'
              namespace: 'default'
              manifests: |
                $(Pipeline.Workspace)/manifests/dev.yml
                $(Pipeline.Workspace)/manifests/service.yml
              imagePullSecrets: 'impatiencepullsecret'
              containers: impatience.azurecr.io/impatience:$(tag)